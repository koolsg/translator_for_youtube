[project]
name = "my-server"
version = "0.1.0"
description = "FastAPI Server"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "google-generativeai",
    "plyer",
    "python-dotenv",
    "youtube-transcript-api",
    "psutil"
]

[dependency-groups]
dev = [
    "pyright>=1.1.407",
    "ruff>=0.14.6",
]
